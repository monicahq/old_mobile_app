import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  header: {
    marginVertical: 10,
    flexDirection: 'row',
    paddingHorizontal: 5,
  },
  headerCentered: {
    alignSelf: 'center',
  },
});
