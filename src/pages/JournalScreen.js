import React from 'react';
import {Text} from 'react-native';
import {SafeAreaView} from 'react-navigation';

export const JournalScreen = ({navigation, banner}) => (
  <SafeAreaView forceInset={{top: 'always'}}>
    <Text>Journal</Text>
  </SafeAreaView>
);
