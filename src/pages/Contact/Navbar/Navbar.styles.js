import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  contactImage: {
    position: 'absolute',
    top: -32,
    borderRadius: 3,
    borderColor: 'white',
    borderWidth: 3,
  },
  leftButton: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingLeft: 12,
  },
});
