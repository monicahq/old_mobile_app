import React from 'react';
import {Text} from 'react-native';
import {SafeAreaView} from 'react-navigation';

export const AddContactScreen = ({navigation, banner}) => (
  <SafeAreaView forceInset={{top: 'always'}}>
    <Text>Add Contact</Text>
  </SafeAreaView>
);
