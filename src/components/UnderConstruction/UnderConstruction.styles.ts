import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    textAlign: 'center',
    fontSize: 18,
    marginTop: 30,
    paddingHorizontal: '20%',
  },
  flexCentered: {
    flex: 1,
    alignItems: 'center',
  },
});
