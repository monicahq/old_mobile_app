import {} from '../reducer';
// import * as types from '../types';

describe('Redux', () => {
  describe('Contacts', () => {
    describe('Reducer', () => {
      it('true', () => {
        expect(true).toBe(true);
      });
    });
  });
});
