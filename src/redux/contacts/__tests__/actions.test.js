import {} from '../actions';
// import * as types from '../types';

describe('Redux', () => {
  describe('Contacts', () => {
    describe('Actions', () => {
      it('true', () => {
        expect(true).toBe(true);
      });
    });
  });
});
