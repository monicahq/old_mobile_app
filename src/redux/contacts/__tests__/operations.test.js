import {} from '../operations';
// import * as types from '../types';

describe('Redux', () => {
  describe('Contacts', () => {
    describe('Operations', () => {
      it('true', () => {
        expect(true).toBe(true);
      });
    });
  });
});
